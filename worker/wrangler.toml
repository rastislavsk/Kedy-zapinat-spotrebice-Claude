# Cloudflare Workers Builds: Version command (PR/non-production branch) musí mať
# --config worker/wrangler.toml (Settings -> Build), lebo Root directory sa naň
# neaplikuje a beží z koreňa repa — pozri worker/README.md#troubleshooting.
name = "pv-proxy"
main = "src/index.js"
compatibility_date = "2026-08-29"

[triggers]
crons = ["*/5 * * * *"]
