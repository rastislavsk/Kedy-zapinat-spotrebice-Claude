# Cloudflare Workers Builds: Root directory musí byť nastavené na "worker"
# (Settings -> Builds), inak build nenájde tento súbor a nasadí to ako static assets.
name = "pv-proxy"
main = "src/index.js"
compatibility_date = "2026-08-29"

[triggers]
crons = ["*/5 * * * *"]
